// This is not used for building, only for deploying! You prob dont need to run this.
plugins {
	id 'maven-publish'
}

version = "0.5.1"

def ENV = System.getenv()

publishing {
	publications {
		maven(MavenPublication) {
			groupId "net.fabricmc.fabric-installer-native-bootstrap"
			artifactId "windows-i686"
			version project.version

			artifact (file("${projectDir}/Win32/Release/fabric-installer-native-bootstrap.exe")) {
				classifier = null
			}
			artifact (file("${projectDir}/Win32/Release/fabric-installer-native-bootstrap.pdb")) {
            	classifier = "pdb"
            }
		}
	}

	repositories {
		if (ENV.MAVEN_URL) {
			maven {
				url ENV.MAVEN_URL
				credentials {
					username ENV.MAVEN_USERNAME
					password ENV.MAVEN_PASSWORD
				}
			}
		}
	}
}